<div class="body_in">
  <div class="container_in body_in">
    <div class="form-container sign-in-container">
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <h1>Iniciar sesion</h1>
        <div class="social-container">
          <a href="#" class="social"><i class="fab fa-facebook-f"></i></a>
          <a href="#" class="social"><i class="fab fa-google-plus-g"></i></a>
          <a href="#" class="social"><i class="fab fa-linkedin-in"></i></a>
        </div>
        <span>o utiliza una cuenta</span>
        <div class="form-inputs">
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email" } %>
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password" } %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable?%>
        </div>
        <a href="#">olvidaste la contraseña?</a>
        <div class="">
          <%= f.button :submit, "Log in", class:"btn" %>
        </div>
      <% end %>
    </div>
    <div class="overlay-container">
      <div class="overlay">
        <div class="overlay-panel overlay-right">
          <h1 id="titulo-alt">Bienvenido!</h1>
          <p>Registrate con nosotros y comienza el cuidado de tu hisotial medico</p>
           <%= link_to "Registrarse", new_user_registration_path, class:"btn"%>
        </div>
      </div>
    </div>
  </div>
</div>
